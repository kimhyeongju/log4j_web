apiVersion: v1
kind: Secret
metadata:
  name: log4j-app-secret
  namespace: log4j-demo
  labels:
    app: log4j-web
type: Opaque
data:
  # base64로 인코딩된 값들 (실제 사용 시 변경 필요)
  # echo -n "webuser" | base64
  SPRING_DATASOURCE_PASSWORD: d2VidXNlcg== # webuser
